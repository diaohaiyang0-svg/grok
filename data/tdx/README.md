# TDX daily bars

`513130_daily.csv` is unadjusted Shanghai ETF daily bars from Tongdaxin via `pytdxdata` (same client as `a-share-radar`).

Refresh:

```bash
pip install -r requirements.txt
python scripts/fetch_513130_tdx.py
```
